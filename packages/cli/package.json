{
  "name": "cli",
  "private": true,
  "description": "Internal CLI build — published via root package",
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node --format esm",
    "dev": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@clack/prompts": "^1.4.0",
    "commander": "^14.0.3",
    "fs-extra": "^11.3.5",
    "picocolors": "^1.1.1",
    "ora": "^9.4.0"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
