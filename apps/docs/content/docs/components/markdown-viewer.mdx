---
title: MarkdownViewer
description: GitHub-style Markdown renderer with GFM, alerts, footnotes, math, safe HTML, tables, task lists, and code blocks.
---

<ComponentPreview name="markdown-viewer-docs-demo" previewClassName="min-h-[380px] items-start p-4" align="start" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx @draco-china/shadcn-pro@latest add markdown-viewer
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Install the following dependencies.</Step>

```bash
bun add react-markdown lucide-react remark-gfm remark-breaks remark-math remark-toc rehype-raw rehype-sanitize rehype-mathjax
```

<Step>Install or copy `pro-base` and `code-viewer` when installing manually.</Step>

<Step>Copy and paste the MarkdownViewer files into your project.</Step>

<ComponentSource name="markdown-viewer" title="components/pro/viewer/markdown/index.tsx" />

Manual installs also need the sibling files used by the entry component:

- `components/pro/viewer/markdown/alert-element.tsx`
- `components/pro/viewer/markdown/alert-style.tsx`
- `components/pro/viewer/markdown/alert.tsx`
- `components/pro/viewer/markdown/block-elements.tsx`
- `components/pro/viewer/markdown/classes.ts`
- `components/pro/viewer/markdown/code.tsx`
- `components/pro/viewer/markdown/components.tsx`
- `components/pro/viewer/markdown/elements.tsx`
- `components/pro/viewer/markdown/github-alerts.ts`
- `components/pro/viewer/markdown/headings.tsx`
- `components/pro/viewer/markdown/inline-elements.tsx`
- `components/pro/viewer/markdown/math-code-blocks.ts`
- `components/pro/viewer/markdown/plugins.ts`
- `components/pro/viewer/markdown/sanitize-schema.ts`
- `components/pro/viewer/markdown/table.tsx`
- `components/pro/viewer/markdown/types.ts`
- `components/pro/viewer/markdown/utils.ts`
- `components/pro/viewer/markdown/viewer.tsx`

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { MarkdownViewer } from "@/components/pro/viewer/markdown"

export default function Example() {
  return (
    <MarkdownViewer
      content="# Hello\n\n> [!NOTE]\n> GitHub-style markdown is supported."
    />
  )
}
```

## API Reference

### MarkdownViewer

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `content` | `string` | — | Markdown string to render. |
| `theme` | `"light" \| "dark"` | `"dark"` | Code block surface theme. Pass this from your app theme when needed. |
| `emptyText` | `string` | `"No markdown content"` | Message shown when `content` is empty. |
| `className` | `string` | — | Additional CSS class for the prose wrapper. |

## Supported Syntax

- CommonMark plus GitHub Flavored Markdown: tables, task lists, strikethrough, autolinks, and footnotes.
- GitHub alerts: `NOTE`, `TIP`, `IMPORTANT`, `WARNING`, and `CAUTION`.
- Table of contents generation from `## Contents`, `## Table of Contents`, or `## TOC` headings.
- Raw HTML rendered through a sanitize pipeline.
- Inline and block math rendered with MathJax.
- Fenced code blocks rendered through CodeViewer, including shared Shiki themes, copy controls, line numbers, folding, and empty/error states.
