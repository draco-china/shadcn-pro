---
title: CodeViewer
description: Read-only syntax-highlighted code viewer powered by Shiki — with One themes, copy button, line numbers, folding, and empty/error states.
---

<ComponentPreview name="code-viewer-docs-demo" previewClassName="min-h-[340px] items-start p-4" align="start" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx @draco-china/shadcn-pro@latest add code-viewer
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Install the following dependencies.</Step>

```bash
bun add shiki lucide-react
```

<Step>Install or copy `pro-base` when installing manually.</Step>

```bash
npx shadcn@latest add badge command dropdown-menu popover separator tooltip
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="code-viewer" title="components/pro/viewer/code/index.tsx" />

Manual installs also need the sibling files used by the entry component:

- `components/pro/viewer/code/classes.ts`
- `components/pro/viewer/code/header.tsx`
- `components/pro/viewer/code/fold-button.tsx`
- `components/pro/viewer/code/table.tsx`
- `components/pro/viewer/code/table-row.tsx`
- `components/pro/viewer/code/status.tsx`
- `components/pro/viewer/code/lines.ts`
- `components/pro/viewer/code/types.ts`
- `components/pro/viewer/code/use-code-viewer.ts`
- `components/pro/viewer/shared/syntax.ts`

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { CodeViewer } from "@/components/pro/viewer/code"

export default function Example() {
  return (
    <CodeViewer
      code={`const hello = "world"`}
      lang="typescript"
      theme="dark"
      showLineNumbers
    />
  )
}
```

## API Reference

### CodeViewer

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `code` | `string` | — | The source code string to display. |
| `lang` | `string` | `"typescript"` | Language identifier for syntax highlighting (e.g. `"tsx"`, `"python"`). |
| `theme` | `"light" \| "dark"` | `"dark"` | Shared One syntax theme. |
| `showLineNumbers` | `boolean` | `true` | Whether to display line numbers. |
| `showHeader` | `boolean` | `true` | Whether to show the filename/language and copy toolbar. |
| `surface` | `"code" \| "embedded"` | `"code"` | Use `"embedded"` when rendering inside another surface. |
| `title` | `string` | — | Optional filename shown in the toolbar. |
| `emptyText` | `string` | `"No code"` | Message shown when `code` is empty. |
| `scrollRef` | `Ref<HTMLDivElement>` | — | Ref forwarded to the scrollable code viewport. |
| `onScroll` | `UIEventHandler<HTMLDivElement>` | — | Scroll handler for synchronizing with another viewer. |
| `className` | `string` | — | Additional CSS class for the root element. |
