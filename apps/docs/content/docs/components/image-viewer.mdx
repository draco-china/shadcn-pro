---
title: ImageViewer
description: Full-screen image previewer with zoom, rotate, drag, and multi-image navigation.
---

<ComponentPreview name="image-viewer-docs-demo" previewClassName="min-h-[340px] items-start p-4" align="start" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx @draco-china/shadcn-pro@latest add image-viewer
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Install the following dependencies.</Step>

```bash
bun add lucide-react
```

<Step>Install or copy `pro-base`. The registry installer includes the private toolbar dependency automatically; fully manual copies also need `components/pro/base/toolbar/`.</Step>

```bash
npx shadcn@latest add badge command dropdown-menu popover separator tooltip
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="image-viewer" title="components/pro/viewer/image/index.tsx" />

Manual installs also need the sibling files used by the entry component:

- `components/pro/viewer/image/classes.ts`
- `components/pro/viewer/image/nav.tsx`
- `components/pro/viewer/image/portal.tsx`
- `components/pro/viewer/image/stage.tsx`
- `components/pro/viewer/image/state.ts`
- `components/pro/viewer/image/types.ts`
- `components/pro/viewer/image/use-image-drag.ts`
- `components/pro/viewer/image/use-image-keyboard.ts`
- `components/pro/viewer/image/use-image-transform.ts`
- `components/pro/viewer/image/use-image-viewer.ts`
- `components/pro/viewer/image/toolbar.tsx`
- `components/pro/viewer/image/toolbar-button.tsx`
- `components/pro/viewer/image/toolbar-items.tsx`
- `components/pro/viewer/image/toolbar-items-center.tsx`
- `components/pro/viewer/image/toolbar-items-left.tsx`
- `components/pro/viewer/image/toolbar-items-right.tsx`
- `components/pro/viewer/image/thumbnails.tsx`

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { ProButton } from "@/components/pro/base/button"
import { ImageViewer } from "@/components/pro/viewer/image"
import { useState } from "react"

export default function Example() {
  const [open, setOpen] = useState(false)

  return (
    <>
      <ProButton onClick={() => setOpen(true)}>Open</ProButton>
      <ImageViewer
        images={["https://example.com/photo.jpg"]}
        open={open}
        onClose={() => setOpen(false)}
      />
    </>
  )
}
```

## API Reference

### ImageViewer

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `images` | `string \| string[]` | — | One or more image URLs to display. |
| `open` | `boolean` | — | Controls whether the viewer is open. |
| `onClose` | `() => void` | — | Called when the viewer is dismissed. |
| `index` | `number` | — | Controlled image index. Use with `onIndexChange`. |
| `initialIndex` | `number` | `0` | Index of the image to show first. |
| `onIndexChange` | `(index: number) => void` | — | Called when navigation changes the active image. |
| `alt` | `string` | `"Image"` | Alt text prefix applied to each image. |
| `container` | `Element \| DocumentFragment \| null` | `document.body` | Portal target. Pass `null` or a custom element to control where the viewer renders. |
| `className` | `string` | — | Additional CSS class for the fullscreen overlay. |
| `toolbarClassName` | `string` | — | Additional CSS class for the toolbar. |
| `thumbnailsClassName` | `string` | — | Additional CSS class for the thumbnails strip. |

## Keyboard Shortcuts

| Key | Action |
| --- | --- |
| `←` / `→` | Previous / Next image |
| `Esc` | Close viewer |
| `Scroll` | Zoom in / out |
